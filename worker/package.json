{
  "name": "portal-notifier-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "vapid": "web-push generate-vapid-keys"
  },
  "dependencies": {
    "pdfjs-dist": "^4.10.38",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
